# Functions

- A function is a block of code which only runs when it is called. You can pass data, known as parameters, into a function. A function can return data as a result.

- we will learn :

1. Creating a Function using def keyword
2. Calling a Function
3. Improving readability, traceability & maintainability using functions
4. Bread down large problems into smaller chunks using functions

## Scopes

- A variable is only available from inside the region it is created. This is called scope. There are two scopes :

1. Local Scope : A variable created inside a function belongs to the local scope of that function, and can only be used inside that function.
2. Global Scope : A variable created in the main body of the Python code is a global
